# /Users/butryan/.config/tmuxinator/pep.yml

name: pep
root: ~/workspace/pep

windows:
  - code: vim
  - terminal: exa --tree --level 2
  - cdd:
      layout: even-horizontal
      panes:
        - cdd
        - cdd
