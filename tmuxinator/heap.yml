# /Users/butryan/.config/tmuxinator/heap.yml

name: heap
root: ~/workspace/HEAP-AL2/src/Aws-ps-heap-main

windows:
  - local: sleep 0.5 && clear && pwd 
  - cdd-heap:
      layout: even-horizontal
      panes:
        - sleep 0.5 && clear && ssh cdd-heap
        - sleep 0.5 && clear && ssh cdd-heap-test
