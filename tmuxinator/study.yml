# /Users/butryan/.config/tmuxinator/study.yml

name: study
root: ~/development

windows:
  - structy:
      layout: even-horizontal
      panes:
        - cd ds_algo_exercises && av && sleep 0.5 && clear 
        - cd ds_algo_exercises && av && sleep 0.5 && clear
  - leetcode:
      layout: even-horizontal
      panes:
        - cd leetcode && av && sleep 0.5 && clear
        - cd leetcode && av && sleep 0.5 && clear
  - cracking:
      layout: even-horizontal
      panes:
        - cd cracking && av && sleep 0.5 && clear
        - cd cracking && av && sleep 0.5 && clear
  - last_algo:
      layout: even-horizontal
      panes:
        - cd last_algo_ever_need && av && sleep 0.5 && clear
        - cd last_algo_ever_need && av && sleep 0.5 && clear
